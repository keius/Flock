import React from 'react';
import {Link} from 'react-router-dom';

const MainBanner = ({currentUser, logout}) => {

  return (
    <div className="main-banner">Banner goes here</div>
  );
};

export default MainBanner;
